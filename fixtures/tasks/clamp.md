Implement `clamp` in `src/tiny_tasks.py`.

Requirements:

- Values below `lower` return `lower`.
- Values above `upper` return `upper`.
- Values inside the inclusive range are returned unchanged.
- If `lower > upper`, raise `ValueError`.
- Do not change the public signature.

Run the relevant tests before finishing.
