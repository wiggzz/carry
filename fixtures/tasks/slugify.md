Implement `slugify` in `src/tiny_tasks.py`.

Requirements:

- Return lowercase ASCII text.
- Transliterate accented Latin characters where Python's standard library supports it.
- Replace each run of non-alphanumeric characters with one `-`.
- Remove leading and trailing separators.
- Return an empty string when no alphanumeric characters remain.
- Use only the Python standard library.

Run the relevant tests before finishing.
