Implement `median` in `src/tiny_tasks.py`.

Requirements:

- Return the middle value for odd-length inputs.
- Return the arithmetic mean of the two middle values for even-length inputs.
- Raise `ValueError` for an empty input.
- Do not mutate the caller's list.
- Do not change the public signature.

Run the relevant tests before finishing.
